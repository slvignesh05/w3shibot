[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "w3shibot"
version = "2.0.5"
description = "Custom test package for private PyPI index"
authors = [{ name = "w3shi" }]
readme = "README.md"
requires-python = ">=3.7"
